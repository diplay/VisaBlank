<%= form_for [@client.manager, @client], html: {class: "form-horizontal"} do |f| %>
  <%= render partial: 'client_info_form', object: f, as: :f %>
  <%= f.submit "Создать", class: "btn btn-success pull-right" %>
<% end %>
